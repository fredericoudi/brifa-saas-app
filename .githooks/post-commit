#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"

if [ "$branch" != "main" ]; then
  exit 0
fi

if [ ! -f ".vercel/project.json" ]; then
  exit 0
fi

if ! command -v npm >/dev/null 2>&1; then
  exit 0
fi

echo "Vercel auto deploy: publicando a branch main..."
npm run deploy:prod
